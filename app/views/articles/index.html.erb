<h1 class="display">Articles</h1>
<div>
  <ul>
    <% @articles.each do |article| %>
     <li> <%= link_to article.title, article_path(article) %> </li>
    <% end %>
  </ul>
  <%= link_to 'Add a new article', new_article_path, {:class => "add"}%>
</div>
